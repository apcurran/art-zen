#!/usr/bin/env bash
# exit on error
set -o errexit

# install node server deps
npm clean-install

# navigate to client dir, and install frontend deps
cd client
npm clean-install
npm run build
cd ../

echo Finished build